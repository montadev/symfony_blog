{% extends ('base.html.twig')%}
{% block body %}
<article>
   <h2>{{article.title}}</h2>
        <div class="metadata">Ecrit le {{article.createdAt | date('d/m/Y')}}
        Ullamco anim deserunt aute ipsum ipsum ex nostrud ut.
        </div>
        <div class="content">
         <img alt="" src="{{article.image}}">
        {{article.content | raw}}
          </div>
</article>
{% endblock %}